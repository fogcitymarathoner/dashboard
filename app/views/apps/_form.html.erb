<%= form_for @app, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :app_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :app_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :inst_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :inst_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :app_ios_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :app_ios_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :app_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :app_name, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                apps_path, :class => 'btn' %>
  </div>
<% end %>
