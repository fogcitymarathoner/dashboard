<%= form_for @page, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :page_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :page_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :page_number, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :page_number, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                pages_path, :class => 'btn' %>
  </div>
<% end %>
