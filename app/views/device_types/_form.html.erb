<%= form_for @device_type, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :device_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :device_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :device_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :device_name, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                device_types_path, :class => 'btn' %>
  </div>
<% end %>
